require "spec_helper"

describe Geojson2image do
  it "has a version number" do
    expect(Geojson2image::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
